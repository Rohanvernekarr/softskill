package graphs;

import java.util.Queue;

public class bfs {
    int V;
    ListGraph g;

    void bfs(int start){
        boolean v = new boolean[V]
        Queue<Integer> q = new LinkedList<>();
        q.add(start);
        v[start] = true;
    }

    
}
